# Contributing

## 开发流程

1. 从 `main` 拉分支；
2. 修改页面、状态、API 或文档；
3. 运行 `pnpm lint` 和 `pnpm build`；
4. 涉及接口时先更新 `yijie-contracts`；
5. 提交 PR。

## Commit 规范

- `feat:` 新功能
- `fix:` 修复
- `docs:` 文档
- `refactor:` 重构
- `test:` 测试
- `chore:` 工程化
