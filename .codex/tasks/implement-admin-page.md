# Task: Implement Admin Page

## Requirements

- Read `AGENTS.md` and `SECURITY.md`.
- Read the Feature Delivery v2 README/HANDBOOK and use a schema v2 Package owned by `36Dge`.
- Verify G0/G1/G2 and every applicable G2C before editing implementation; remain inside the current Authorization scope.
- Use generated contracts when API data is involved.
- Keep authorization truth in `yijie-api`.
- Complete G3 for the page Slice and G4 for engineering closure; never sign or self-approve a Gate.
