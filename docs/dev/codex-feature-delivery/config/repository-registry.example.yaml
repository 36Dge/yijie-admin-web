schema_version: 1
kind: FeatureDeliveryRepositoryRegistry

# `id` must match project.repository.current_id in `.feature-delivery.yaml`.
repositories:
  - id: example-app
    name: example-app
    url: https://example.invalid/acme/example-app.git
    root: "."
    kind: current
